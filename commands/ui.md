---
description: Open the Tasky dashboard, starting the local server if it is not running
allowed-tools: Bash("${CLAUDE_PLUGIN_ROOT}/bin/tasky" ui)
---

!`"${CLAUDE_PLUGIN_ROOT}/bin/tasky" ui`

Reply with only the dashboard URL printed above, on one line, and nothing else.
